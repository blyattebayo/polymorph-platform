<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Маршрутизация
    |--------------------------------------------------------------------------
    |
    | Маршруты ядра описаны нативным Laravel в platform/routes, маршруты
    | расширений приезжают типизированными объектами SDK и монтируются
    | в зону, выведенную из id расширения.
    |
    */

    // Каталог роут-файлов ядра. Их список и порядок задаёт
    // Routing\RoutingServiceProvider::CORE_FILES — здесь только корень,
    // чтобы тесты и хост могли подменить каталог целиком.
    'path' => dirname(__DIR__).'/routes',
];
